---
import Layout from '@layouts/Base.astro';
import { EXAMPLES } from '@examples/registry';
import { WipVersionsBadge } from '@components/WipVersionsBadge';

export const prerender = true;
---

<Layout>
  <main class="home-shell l-stack l-space-m">
    <WipVersionsBadge client:load />
    <h1 class="u-step-2">Pick a game</h1>
    <div class="example-links" role="list">
      {Object.entries(EXAMPLES).map(([id, ex]) => (
        <a href={`/play/${id}`} class="example-link" role="listitem">{ex.label}</a>
      ))}
    </div>
  </main>
</Layout>

<style>
  .home-shell {
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100dvh;
    padding: 2rem 1rem;
    box-sizing: border-box;
    text-align: center;
  }

  .home-shell > :first-child {
    margin-top: auto;
  }

  .home-shell > :last-child {
    margin-bottom: auto;
  }

  .example-links {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    width: 100%;
    max-width: 320px;
  }

  .example-link {
    display: block;
    padding: 0.75rem 1.25rem;
    font-size: 1.1rem;
    font-weight: 600;
    text-decoration: none;
    text-align: center;
    border-radius: 8px;
    background-color: var(--gray-3, #eee);
    color: var(--gray-12, #111);
    border: 1px solid var(--gray-6, #ccc);
    transition: background-color 0.15s ease, border-color 0.15s ease;
  }

  .example-link:hover {
    background-color: var(--gray-4, #ddd);
    border-color: var(--gray-8, #999);
  }
</style>
